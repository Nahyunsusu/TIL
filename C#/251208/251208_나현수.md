개인 블로그 주소 : https://velog.io/@trefdscxz/posts

# 1. 주 기억장치와 보조 기억장치
- 주기억장치(Computer Memory, Primary Memory) :
  
    주기억장치는 컴퓨터 내부에서 현재 CPU가 처리하고 있는 내용을 저장하고 있는 기억장치이다. 대표적으로 RAM, ROM이 있다.

- 보조기억장치(Auxiliary Memory, Secondary Memory)

    보조기억장치는 물리적인 디스크가 연결되어 있는 기억장치이다. 주기억장치보다는 느리지만 컴퓨터가 전원을 끄더라도 저장된 데이터가 사라지지 않고 영구적으로 보관할 수 있는 장치이다.

# 2. 응용 프로그램과 시스템 프로그램
- 응용 프로그램 : 
  
  크롬, 워드 비주얼 스튜디오 언리얼 게임 등등 여러 프로그램
- 시스템 프로그램 : 
    
    Windows, Linux, IOS 등이 있지만 Windows밖에 써보지 않았다.

# 이진수 및 바이트 변환

## 10to2
    824748 -> 11001001010110101100
## 2to10
    101101 -> 45

##
    byte = 8bit ex) 12byte = 96bit

# Windows32bit, 64bit
두 개는 메모리 주소를 얼마나 큰 범위까지 표현할 수 있는지에 차이가 있다. 32bit
- 32bit OS 
  - 주소를 32bit로 표현 이론상 최대 메모리는 4GB
- 64bit OS
  - 주소를 64bit로 표현 이론상 메모리를 TB까지 커버 가능하지만 제한을 두고있다.



# C#의 컴파일 과정
   1. *.cs 파일에 C#으로 코드를 작성한다.
   2. C# 컴파일러 csc.exe가 *.cs 파일을 컴파일을 한다.
   3. 컴파일러의 결과물로 .exe나 .dll이 나온다 확장자가 exe이지만 바로 실행 가능한 결과물이 아닌 CIL(Common Intermediate Language)로 중간언어이다.
   4. *.exe 파일을 실행
   5. 실행하기 위해서 CIL코드가 CLR 내부의 Class Loader에 의해서 CIL의 클래스들의 레이아웃 로드와 메타데이터 및 사용된 클래스 라이브러리 등을 로드하여 각종 과정을 거친 뒤 메모리에 로드
   6. 메모리에 올라간 CIL를 컴퓨터가 이해할 수 있는 코드로 변환해주는 역할을 해주는 것이 JIT 컴파일러이다. (CLR에 포함된다.)
   7. JIT 컴파일러에 의해 컴파일 된 어셈블리어는 다음번 실행을 위해 메모리에 캐시에 올리며 실행한다.


![alt text](image.png)