# 과제1 입력받은 횟수만큼 반복하는 기능 제작
```c#
using System;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Runtime.InteropServices;
using System.Security.Cryptography.X509Certificates;

namespace _1209_Temp
{
    public static class SwitchExample
    {
        public static void Main()
        {
            Console.Write("몇회 반복하시겠습니까? : ");
            string k = Console.ReadLine();

            bool isConverted = int.TryParse(k, out int num);

            for (int i=0;i<num;i++)
            {
                Console.WriteLine($"{i+1} 회 반복중입니다");
            }
        }
    }
}
```

# 과제2 구구단 기능 제작
```c#
using System;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Runtime.InteropServices;
using System.Security.Cryptography.X509Certificates;

namespace _1209_Temp
{
    public static class SwitchExample
    {
        public static void Main()
        {
            int num = 0;
            while (true) 
            {
                Console.Write("1부터 9 사이의 숫자를 입력하세요 : ");
                string str = Console.ReadLine();

                if (int.TryParse(str, out num))
                {
                    if (num >= 1 && num <= 9)
                    {
                        break;
                    }
                }
                Console.WriteLine("1에서 9 사이의 정수를 다시 입력해 주세요.");
            }

            for (int i=1;i<=9;i++)
            {
                Console.WriteLine($"{num} * {i} = {num * i}");
            }
        }
    }
}
```
# 과제3 별찍기
```c#
using System;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Runtime.InteropServices;
using System.Security.Cryptography.X509Certificates;

namespace _1209_Temp
{
    public static class SwitchExample
    {
        public static void Main()
        {
            int num = 5;

            for(int i=0;i<5;i++)
            {
                for(int j=0;j<=i;j++)
                {
                    Console.Write("*");
                }
                Console.WriteLine();
            }
        }
    }
}
```
![alt text](1210_1-1.png)
```
using System;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Runtime.InteropServices;
using System.Security.Cryptography.X509Certificates;

namespace _1209_Temp
{
    public static class SwitchExample
    {
        public static void Main()
        {
            int num = 5;

            for(int i=0;i<5;i++)
            {
                for(int k = 5;k>=i;k--)
                {
                    Console.Write(" ");
                }
                for (int j=0;j<=i;j++)
                {
                    Console.Write("*");
                }
                Console.WriteLine();
            }
        }
    }
}
```
![alt text](1210_2-1.png)